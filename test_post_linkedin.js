require('dotenv').config();const { supabase } = require('./database/db');const { publishPost } = require('./services/linkedin');const path = require('path');async function runTest() {  const { data: user } = await supabase.from('users').select('access_token, linkedin_id').limit(1).single();  const text = 'Navigating the complexities of modern software development demands a rock-solid foundation. That\\'s why I recently committed to reinforcing a core programming language, and I\\'m pleased to share the result: I\\'ve successfully completed the HackerRank Java (Basic) certification! This milestone represents a dedicated effort to validate and deepen my understanding of fundamental Java principles.';  const img = { path: path.join(__dirname, 'uploads/42786995-4c0a-4ba3-87ce-f0eae8e93e5e.jpeg'), mimetype: 'image/jpeg' };  try {    const id = await publishPost(user.access_token, user.linkedin_id, text, '#Java #Coding', [img]);    console.log('LinkedIn post successful! ID:', id);  } catch (e) {  console.error('LinkedIn Error:', e.response?.data || e.message); }}runTest();
